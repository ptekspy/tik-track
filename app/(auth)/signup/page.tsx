import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up | TikTrack',
  description: 'Create your TikTrack account',
};

export default function SignupPage() {
  return <SignupForm />;
}
